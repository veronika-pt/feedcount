/**
 * @typedef {Object} DailyInput
 * @property {number} formulaConsumedMl
 * @property {number} formulaFeedsLeftToday
 */

/** @type {DailyInput} */
export const defaultDailyInput = {
	formulaConsumedMl: 0,
	formulaFeedsLeftToday: 0
};